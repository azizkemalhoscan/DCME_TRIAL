function refreshForm(innerHTML) {
  const newSurveyQuestionForm = document.getElementById('new_survey_question');
  newSurveyQuestionForm.innerHTML = innerHTML;
}

function addSurveyQuestion(questionHTML) {
  const questions = document.getElementById('questions');
  questions.insertAdjacentHTML('beforeend', questionHTML);
}

<% if @survey_question.errors.empty?  %>

	document.getElementById('questions-container').innerHTML = '<%= j render "surveys/questions-container", survey: @survey %>'
  // addSurveyQuestion('<%= j render "survey_questions/add_question", question: @survey_question, survey: @survey_question.survey, counter: 5 %>');
  refreshForm('<%= j render "survey_questions/new_question", question: SurveyQuestion.new, survey: @survey %>');

<% end %>

loadOptionSelection = () => {
  checkBoxes = document.querySelectorAll(".form-check");

  checkBoxes.forEach((box) => {
    box.addEventListener('click', (event) => {
      checkBoxes.forEach( box => box.classList.remove("selected-option") )
      event.currentTarget.classList.add("selected-option")
    })
  })
}

loadOptionSelection();
