<li>
  <div id="q-<%= question.id %>">
  <%= question.question %>
      <%= link_to 'Edit <i class="fas fa-edit"></i>'.html_safe, edit_survey_question_path(question.id), remote: true  %>
  <%= link_to 'Delete <i class="fas fa-trash-alt"></i>'.html_safe, survey_question_path(survey, question.id), method: :delete, data: {confirm: "Are you sure?"} %>
  </div>
</li>
